{
  "name": "ncdprime",
  "version": "0.1.0",
  "description": "Modern NCD (Normalized Compression Distance) utility + library",
  "type": "module",
  "bin": {
    "ncdprime": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["ncd", "compression", "unsupervised", "clustering"],
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
